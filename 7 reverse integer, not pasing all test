class Solution:
    def reverse(self, x: int) -> int:
        sign = -1 if x<0 else 1
        n = abs(x)
        reversed = 0

        while n != 0:

            digit = n % 10

            reversed = reversed * 10 + digit

            n = n // 10

        return sign * reversed
