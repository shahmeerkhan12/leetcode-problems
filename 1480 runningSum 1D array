class Solution {
    public int[] runningSum(int[] nums) {
        int[] myNums = new int[nums.length];
        myNums[0] = nums[0];

        for (int i = 1; i < nums.length; i++) {
            myNums[i] = nums[i] + myNums[i - 1];
        }
        return myNums;
    }
}
