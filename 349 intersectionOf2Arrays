class Solution:
    def intersection(self, nums1: List[int], nums2: List[int]) -> List[int]:
        cop = nums2
        for num in nums1:
            if num in nums2:
        res = list(set(nums1) & set(nums2))
        return res
